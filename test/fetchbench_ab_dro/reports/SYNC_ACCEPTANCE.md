# test 同步验收（2026-09-13）

仅同步到独立功能分支，基于 v7 文档提交 `68df8e4`；主目录的 v7 实现不变。

## 已执行

- `audit_package.py`：53 个远端源码文件 SHA256 全部一致；12 个生成时冻结 hash 全部一致。
- 所有 Python 文件静态解析、所有 shell 文件 `bash -n` 通过，不导入 Isaac Gym。
- `python -m unittest discover -s tests -v`：10/10 通过，包括配置、候选预算、最终高度口径、
  NaN/缺失分母、坐标转换往返、旧 AB 路径回归、不同 checkpoint 明示。
- 新目录导出测试通过；导出工具拒绝覆盖任何已存在的目标目录。
- 新统计脚本在远端对现有实验执行了**只读重统计**，没有启动生成或仿真：

|方法|计划|NaN|环境筛除|实际执行|最终高度成功|严格成功|缺失|
|---|---:|---:|---:|---:|---:|---:|---:|
|A|1376|8|0|1368|123|16|0|
|B|1376|17|0|1359|138|14|0|
|DRO|22016|0|10509|11507|1534|527|0|

三场景计数与已有核验报告一致，完成状态为 true。
A/B 来源为有限姿态验证器汇总；DRO 在此次核验中逐条读取 11507 个实际 trial。

## 明确未执行

- 没有重新进行 SAM3D 重建、GPU 抓取生成、CPU/GPU PhysX 或视频录制。
- 新分阶段入口只做静态/计划测试，未在新的空白节点上进行端到端部署验收。
- 不声称含外部模型、场景资产、手资产、运行库的完整开箱即用发布。
- 不修改模型训练、v7 冻结配置、正式统计门限或远端原始候选。

系统默认 Python 缺少 PyYAML，首次配置测试失败；改用已有 Dexgrasp 环境运行后 10 项全部通过。
这属于离线依赖问题，不是实验生成或仿真失败。

Git 空白审查：历史 `FetchBase.yaml:20` 带行尾空格，`fetch_ptd.py` 末尾带空行；
为维持远端源码逐字节 SHA256 一致，原样保留。新增入口/测试/说明文件无此类空白错误。
